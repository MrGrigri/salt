{
  "name": "@local/angular",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start:angular-web-standalone": "ng serve --project angular-web-standalone",
    "build:salt-angular": "ng build salt-angular",
    "build:salt-angular:prod": "ng build salt-angular --configuration=production",
    "build:salt-angular:prod:watch": "ng build salt-angular --configuration=production --watch",
    "build:salt-angular-standalone": "ng build salt-angular-standalone",
    "build:salt-angular-standalone:prod": "ng build salt-angular-standalone --configuration=production",
    "build:salt-angular-standalone:prod:watch": "ng build salt-angular-standalone --configuration=production --watch",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "dependencies": {
    "@richkode/salt": "^0.0.1"
  },
  "devDependencies": {
    "ng-packagr": "^17.3.0"
  }
}
